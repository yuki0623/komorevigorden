.side-bar
  .side-bar__header
    .side-bar__header__items
      .user
        %p host
        -# = current_gest.name
      .icons
        -# = link_to new_group_path do
        = icon('far','edit')
        -# = link_to edit_user_path(current_user) do
        = icon('fas','cog', class: 'icon')

  .side-bar__talk-list
    -# - current_user.gests.each do |gest|
    .container
      .door-flame 
        .side-bar__talk-list__summary
          -# = link_to group_messages_path (group) do
          .name
            -# = gests.name
            %p gest2とのtalk
            %p gest3とのtalk
          .latest-message
            -# = gests.show_last_message
            %p 最新のメッセージはありません
            %p 最新のメッセージはありません

    